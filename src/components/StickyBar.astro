---
// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Thoys Inc.
import { SITE, type Lang } from '../data/site';
import { CTA } from '../data/cta';
import { href } from '../lib/urls';
import LangToggle from './LangToggle.astro';

interface Props {
	lang: Lang;
	altHref: string;
	section: 'posts' | 'gloss' | 'consult' | 'series';
}
const { lang, altHref, section } = Astro.props;
const s = SITE[lang];
---

<div class="stickybar" id="sticky">
	<div class="inner">
		<span class="title">{s.brand}</span>
		<nav class="navlinks">
			<a href={href(lang)} aria-current={section === 'posts' ? 'true' : 'false'}>{s.navPosts}</a>
			<a href={href(lang, 'glossary')} aria-current={section === 'gloss' ? 'true' : 'false'}>
				{s.navGloss}
			</a>
			<a href={href(lang, 'series')} aria-current={section === 'series' ? 'true' : 'false'}>
				{lang === 'ko' ? '연재' : 'Series'}
			</a>
			<a href={href(lang, 'consult')} aria-current={section === 'consult' ? 'true' : 'false'}>
				{CTA[lang].navConsult}
			</a>
		</nav>
		<LangToggle lang={lang} altHref={altHref} />
	</div>
</div>
